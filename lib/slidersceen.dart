import 'package:flutter/cupertino.dart';

class SliderScreeen extends StatefulWidget {
  const SliderScreeen({Key? key}) : super(key: key);

  @override
  State<SliderScreeen> createState() => _SliderScreeenState();
}

class _SliderScreeenState extends State<SliderScreeen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
